//
//  ContentView.swift
//  mini_challenge
//
//  Created by MAY 04 on 14/9/26.
//

import SwiftUI

struct ContentView: View {
    let studentName = "Vo Chau Anh"
    let studentID = "SESEIU24001"
    let age = "19"
    let gpa = "1"
    let isStudent = "true"
    
    var body: some View {
        VStack {
            Image(systemName: "person.circle.fill")
              .resizable()
              .scaledToFit()
              .frame(width: 120, height: 120)
              .foregroundColor(.gray)
            Text("Student Profile")
                .font(.largeTitle)
                .bold()
            Text("Name: \(studentName)")
            Text("Student ID: \(studentID)")
            Text("Age: \(age)")
            Text("GPA: \(gpa)")
            Text("Student: \(isStudent)")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
